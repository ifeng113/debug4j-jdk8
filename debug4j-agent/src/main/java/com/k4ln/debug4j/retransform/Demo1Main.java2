//
// Source code recreated from a .class file by IntelliJ IDEA
// (powered by FernFlower decompiler)
//

package com.k4ln.demo;

import cn.hutool.core.util.RandomUtil;
import com.sun.tools.attach.AgentInitializationException;
import com.sun.tools.attach.AgentLoadException;
import com.sun.tools.attach.AttachNotSupportedException;
import java.io.IOException;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class Demo1Main {
    private static final Logger log = LoggerFactory.getLogger(Demo1Main.class);

    public Demo1Main() {
    }

    public static void main(String[] args) throws IOException, AttachNotSupportedException, AgentLoadException, AgentInitializationException {
        for(int i = 0; i < 1000; ++i) {
            logNumber(i);
            if (i == 999) {
                i = 0;
            }
        }

    }

    private static void logNumber(int i) {
        try {
            Dog dog = Dog.builder().name(RandomUtil.randomNumbers(4)).age(i).build();
            Thread.sleep(3000L);
            log.info("Retrasnform java random tid:{} pid:{} index:{} dog:{}", new Object[]{Thread.currentThread().getId(), ProcessHandle.current().pid(), i, dog.toString()});
        } catch (InterruptedException var2) {
            InterruptedException e = var2;
            throw new RuntimeException(e);
        }
    }
}
